# v80 模块化多谐波实体线圈 10,000 候选搜索验收（2026-08-25）

## 验收结论

本轮已按独立模块几何执行 `10,000/10,000` 个实体候选搜索，未捕获异常为 `0`。
搜索不围绕 v77 seed 2 或 seed 170 微调，也不按装置家族名称路由。

本轮没有搜索到通过八周回候选绑定磁力线门的装置，因此没有可行聚变装置可晋级。
证据走得最远的候选为 **v80 seed 9283**；它是当前最优失败候选，不是可行候选。

## 实体线圈表示

每个候选包含：

- `16–32` 个分布式有限段基线圈；
- `3–8` 个独立有限段多谐波绕组模块；
- 每个模块独立的相位、电流大小与正负、螺旋旋向、极向周期、绕组半径；
- 每个模块独立的相位调制、径向谐波、垂向谐波、谐波相位和环向模数；
- 候选绑定导体半径、匝数、电流密度上限和磁应力上限。

模块中心线重新离散为三维有限线段，并重新计算候选绑定 Biot–Savart 场；不是对抽象
等离子体体或参数表单独评分。

## 10,000 搜索协议

所有候选经过相同的第一层：

1. 图拓扑编译；
2. 端口到实体部件实现；
3. 候选绑定有限线圈场缓存；
4. 12 点 Biot–Savart 旋转变换代理与场纹波代理；
5. 有限场、D-T 功率平衡代理、导体电流密度、磁应力、排热能力快门。

然后按封存排名执行：

- 前 `128`：四周回、每周 `180` 步的三起点 RK4 磁力线门；
- 前 `8`：八周回、每周 `360` 步的三起点 RK4 复核；
- 只有八周回复核未判失败者才允许进入昂贵粒子与后续闭合。

运行结果：

- 搜索候选：`10,000`
- 未捕获异常：`0`
- 四周回 `unknown`：`2`（seed 9283、seed 4148）
- 八周回 `unknown`：`0`
- 装置家族路由：`false`
- 实测进程运行时间：约 `16 min 14 s`（24 Julia threads）

## 当前最优失败候选：seed 9283

实体参数：

- 主半径：`1.805829 m`
- 小半径：`0.521315 m`
- 基线圈：`32`
- 基线圈电流：`45,751.813 A`
- 匝数：`13`
- 线圈间隙：`0.362643 m`
- 导体代理半径：`0.048909 m`
- 独立多谐波模块：`7`

绑定运行点：

- 总离子密度：`4.023142e20 m^-3`
- 离子温度：`18.0052 keV`
- 注入加热：`96.6528 MW`
- 排热能力：`1,690.804 MW`

非粒子门：

- 有限磁场：`pass`
- D-T 功率平衡代理：`pass`
- 导体电流密度：`pass`
- 磁应力：`pass`
- 排热能力：`pass`
- 碰撞less粒子保持：`unknown`（磁力线门先失败，未授予粒子信用）

磁力线证据：

- 四周回：旋转变换 `0.022689`，径向摆幅 `0.115922`，结论 `unknown`
- 八周回：无直接逃逸，旋转变换 `0.020950`，径向摆幅 `0.356804`
- 八周回 classification：`excessive_flux_surface_radial_excursion`
- 八周回结论：`complete/fail`

因此 seed 9283 是“5 个非粒子门通过、四周回未失败、八周回失败”的走得最远候选。

## 工件与哈希

- 工件：`runs/physical_device_v80_20260825/modular_multiharmonic_search_10000.json`
- 文件 SHA-256：`6758572770C0178C40163BC52CC5615CAEDC57FC8FC4A087BE51714886742557`
- result hash：`46aba8bd0cb10ee821cb32fd3e7e9765f6e055fa403f07c2b59c338acd99af75`
- 工件大小：`7,522,643 bytes`

## 回归验收

v71–v80 定向回归 `122/122` 通过，覆盖：

- 实体部件实现与失败闭合；
- 候选绑定场、粒子和等离子体屏；
- 主候选链接入；
- 开放场/闭合场输运门；
- 单谐波、局部、多谐波和独立模块化线圈搜索；
- v80 三阶段搜索、哈希工件和无装置家族路由。

## v80 三维状态实验台

- 本地入口：`http://127.0.0.1:8197/`
- 当前数据：v80 seed 9283 与 10,000 搜索工件
- 页面明确显示：`v80 最优失败候选`、八周回 `fail`、粒子保持 `未验收`
- 可切换：实体装置、候选绑定磁场采样、未验收粒子探针、等离子体绑定状态
- Playwright：四个图层可切换，控制台 `0 error / 0 warning`
- 截图：`output/playwright/physical_frontier_v80_desktop.png`

该实验台没有把调度用 `n=1, steps=1` 粒子探针显示成保持率证据；粒子动画只有在磁面
长期门通过并执行可授信粒子群后才能晋级为模拟证据。

## 边界与下一步

本轮证明的是“实体模块化线圈 10,000 搜索能力已存在并完成验收”，不是“找到可行聚变
装置”。当前主阻塞是八周回磁面径向有界性。下一步应使用 seed 9283 和 seed 4148 的
失败证据训练/构造磁面质量目标，并引入 Poincare 截面、有限压力平衡与优化器；在闭合场
长期有界之前，不应启动大粒子群，也不应把示意动画当作粒子保持证据。
