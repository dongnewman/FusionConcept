# Real candidate uniform C2 chain v1

The real fixed-panel adapter binds `closed_candidate_pool24` and `open_candidate_mirror_low_force` to the same `C2CandidateStatePackageV1` and the same four required gates: Stage 3 residual closure, Stage 4 stability, engineering, and independent evidence.

The source inventories currently provide no complete numerical tuple for the 29 common state, actuator, efficiency, and power-ledger slots used by this adapter. Missing values are represented as JSON `null`; they are never replaced with zero, a reference-device value, or a manufactured default.

Both candidate conclusions therefore remain `unknown`, both completeness states remain `incomplete`, and neither evaluation terminates. The open candidate retains its local two-coil vacuum minimum-B failures as non-terminal narrow failures. Those failures do not falsify the broader candidate or complete Stage 4.

The generated artifact carries candidate-bound acquisition tasks for every missing v68 obligation and Stage 4 operator. It is readiness evidence only and grants no physical C2 or promotion credit.

The executable longitudinal readiness contract expands those coarse obligations into exactly 56 inputs: 10 initial conditions and 46 module parameters. Every value must carry the declared unit, a complete evidence status, an allowed real source kind, and a source-result hash. Reference-device templates, family defaults, and manufactured values are rejected from real-candidate execution.
